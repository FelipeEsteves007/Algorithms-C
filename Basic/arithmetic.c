#include <stdio.h>

int main (){

    int x = 11;
    int y = 2;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;

    // z++;
    // z--;
    // z = z + 1;
    // z += 1;
    // z -= 1;
    // z *= 2;
    z /= 1;

    printf("%d", z);

    return 0;
}